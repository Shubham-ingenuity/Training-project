import { GrabberDirective } from './grabber.directive';

describe('GrabberDirective', () => {
  it('should create an instance', () => {
    const directive = new GrabberDirective();
    expect(directive).toBeTruthy();
  });
});
